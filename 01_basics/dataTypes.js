"use strict";  // treat all JS code as newer version 

// alert(3+3)  //it will not show pop-up when we will run as we are using node js, not browser

console.log(3
    + 3)  //code readability should be high


let name="deepak"
let age=18
let isLoggedIn=false

// number => 2 to power 53
// bigint => for declaring big integer especially in trading and stock market
// string => ""
// boolean => true/false
// null => standalone value
// undefined => that means value is not assigned
// symbol => used for uniqueness


// object

console.log(typeof "deepak");  //string

console.log(typeof age);  //number

console.log(typeof null);  // null is a object so object will print on the screen

console.log(typeof undefined);  // undefined is itself of type so undefined will print on screen